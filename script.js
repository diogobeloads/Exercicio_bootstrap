document.addEventListener("DOMContentLoaded", function () {
  $(".carousel").carousel({
    interval: 1000, // Tempo em milissegundos entre os slides (ajuste conforme necessário)
    pause: "hover", // Pausa a reprodução do carrossel ao passar o mouse sobre ele
  });
});
